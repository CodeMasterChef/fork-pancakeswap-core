{
  "name": "fork-pancakeswap-core",
  "version": "1.0.0",
  "description": "This is PancakeSwap Factory which forked from https://github.com/pancakeswap/pancake-swap-core.",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --reset",
    "migrate:bsctestnet": "truffle migrate --reset --network bsctestnet",
    "verify": "truffle run verify PancakeFactory --network bsctestnet"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.2.6",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "truffle-plugin-verify": "^0.5.7"
  }
}
